export class City {
  name: string;
  state: string;
  country: string;

  constructor(name: string, state: string, country: string) {
    this.country = country;
    this.state = state;
    this.name = name;
  }
}
